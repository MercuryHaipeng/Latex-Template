% 下面的手动设置可以根据需要启用，会覆盖上文 noabbrev 选项
% \crefname{figure}{Fig.}{Figs.}  % 插图标题 Figure 变成缩写 Fig
% \crefname{table}{表}{表}  % 设置 cleveref 中 table 的名称
% 设置图标题格式为粗体 Fig.x.，否则默认为 Figure x: 
% \captionsetup[figure]{labelfont=bf, labelsep=period, name={Fig.}}  

% \mathrm{d} 生成直立的 d, 在数学中, 变量：斜体; 运算符/常量：直立体
% \mathop{} 创建一个数学运算符, {} 是空内容, 仅用于“占位”, 作用是让 TeX 按运算符规则处理后面的间距
% \! 表示一个负的数学间距, 作用：把 d 拉近后面的变量
\newcommand{\diff}{\mathop{}\!\mathrm{d}}

% 在编译时，只处理（只排版）指定的 \include 文件，其余被 \include 的文件全部跳过，页码会按照编译次序依次跳过
% \includeonly{chapter1} 
% \includeonly{chapter2} 
